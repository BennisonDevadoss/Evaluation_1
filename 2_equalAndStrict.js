const a = 2;
const b = '2';

console.log(a == b);    // This equality operator only checks the values. 
console.log(a === b);    // This string equality operator fist check the two operand datatype afterthat it will check the operands value. 

